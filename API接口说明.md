# 数据更新接口说明

程序启动后，接口地址使用浏览器地址栏中的端口。例如页面地址为 `http://127.0.0.1:35117/`，接口根地址就是 `http://127.0.0.1:35117`。

## 查询系统状态

`GET /api/v1/status`

返回岗位总数、岗位簇数、技术领域数、技能数、最近一次导入和最近一次网页操作结果。

## 新增或更新 JD

`POST /api/v1/jobs/import`

请求头：`Content-Type: application/json`

请求体可以直接是数组：

```json
[
  {
    "job_id": "JD-001",
    "原始岗位名": "AI Agent开发工程师",
    "标准岗位名称": "大模型与智能体开发",
    "岗位簇": "大模型与智能体开发",
    "企业": "示例企业",
    "岗位职责": "负责Agent平台研发",
    "任职要求": "熟悉Python、RAG和LangChain"
  }
]
```

也可以使用对象形式：

```json
{"jobs": [{"job_id": "JD-001", "原始岗位名": "AI Agent开发工程师"}]}
```

返回值中的 `added`、`updated`、`unchanged` 分别表示新增、更新和无变化的数量。`job_id`、`id` 或 `JD编号` 应保持稳定；同一岗位后续修改时继续使用原编号。

## Excel 更新

网页中的“导入 / 更新并提取技能”就是 Excel 更新接口。选择第一组最新的真实 JD 库和岗位名称标准化表即可。重复导入相同文件不会产生重复岗位。

## 数据保存位置

数据保存在程序旁的 `data/challenge_cup.db`。刷新页面、关闭浏览器或退出程序不会清空已导入数据。需要给组员复制已有成果时，应复制整个交付文件夹。
