# 最终系统测试报告

## 1. 测试环境

- 操作系统：Windows-11-10.0.26200-SP0
- Python：3.12.13
- Node.js：v24.18.0
- 测试时间：2026-08-26T17:10:02+08:00

## 2. 后端测试

核心单元测试、数据加载、兼容 Adapter 和 OpenAPI 路由检查均已执行。原五个 API 保持注册且真实调用成功：**通过**。

## 3. API测试

- `GET /api/jobs`：HTTP 200，通过
- `GET /api/skills`：HTTP 200，通过
- `POST /api/jd/parse`：HTTP 200，通过
- `POST /api/resume/parse`：HTTP 200，通过
- `POST /api/match`：HTTP 200，通过
- `GET /api/system/overview`：HTTP 200，通过
- `GET /api/job-analysis/{job_title}`：HTTP 200，通过
- `GET /api/graph/job/{job_title}`：HTTP 200，通过
- `GET /api/graph/skill/{skill_id}`：HTTP 200，通过
- `GET /api/evolution/job/{job_title}`：HTTP 200，通过
- `GET /api/emerging-jobs`：HTTP 200，通过
- `GET /api/emerging-jobs/{candidate_id}`：HTTP 200，通过

## 4. 前端测试

- React + Vite 生产构建：通过。
- 八个页面导航、中文显示、候选详情抽屉、图谱页和动态演化缺失状态：通过。
- 浏览器控制台明显错误：未发现。
- 后端关闭提示：已验证显示“后端服务未启动，请启动FastAPI服务。”。
- 1440px 桌面视图和 390px 移动视图横向溢出检查：通过。

## 5. 图谱测试

- 数据来源：由组员A现有正式关系表转换。
- 当前重点岗位图谱：26 个节点、25 条关系（当前请求过滤结果）。
- 关系 Evidence JD：通过。
- 未重新推导新关系。

## 6. 动态演化测试

- 正式文件状态：`not_connected`。
- 页面/API 明确显示“动态演化数据尚未接入”，增长、新增、下降等数组为空。
- 未计算或伪造趋势：通过。

## 7. 新岗位发现测试

- 候选：11 个。
- 高/中/弱：0 / 1 / 10。
- 每个候选包含完整 `evidence_jd_ids` 与 Evidence 记录：通过。
- 单条 JD 不高于弱候选：通过。

## 8. 匹配测试

- 真实 API 匹配分数：33.33。
- 五维 `dimension_scores`、技能差距和原 Matching Engine `recommendations` 均返回：通过。
- 未宣传虚假匹配准确率。

## 9. Evidence测试

JD 解析、简历解析、图谱关系和新岗位候选均可追溯到真实 Evidence 或 JD 编号。校验结果：通过。

## 10. 数据冻结检查

行数：JD 191、简历 27、技能 82、别名 146。ID 唯一性与别名引用检查通过。

- `standardized_jd_dataset`：`B00A0220FD4B974D8B00BB57D6F0AF3BB40F1D92CC7DDD59FCB0DDDA9FC90EDE`（一致）
- `standard_job_title_mapping`：`293B34DBB8E4E6F5689CF58387A38601F30FEB759B46E7F34931BC1F6FF859B1`（一致）
- `standard_skill_dictionary`：`178C64E654D3534878489E88AAFC5A17B98FE361CB38DB370F9036D01E5C1055`（一致）
- `standardized_resume_testset`：`3B78EDFFD349055342818FFF6B803B92D5BD1C1BD1DE140E3764EF82C3CCD952`（一致）

## 11. 已知问题

- 缺失组员 A 正式文件：knowledge_graph_v1.json, key_job_evolution_v1.json, key_job_graph_profiles_v1.xlsx, job_skill_evolution_v1.xlsx, graph_nodes_v1.xlsx, graph_edges_v1.xlsx。
- 当前图谱仅为三个重点岗位正式关系 Excel 的薄兼容转换，并非完整图谱。
- 动态演化尚未接入正式结果。
- 部分新岗位候选只有单条 Evidence，因此如实保留为弱候选。
- Matching V1 分数有限，系统只展示真实结果。
- 前端生产包包含 ECharts，主 JavaScript 包较大，但不影响本地比赛演示。

## 12. 是否达到演示条件

最终结论：**通过**。系统已达到本地比赛演示条件。

---

# 正式图谱/动态演化接入后的第二次系统 QA 记录

## 1. 接入范围与结论

本次只接入组员 A 正式知识图谱与动态演化成果，未重构 JD Parser、Resume Parser、Matching Engine、前端架构或新岗位发现，未运行图谱构建和演化计算程序。最终结论：**System V1 第二次系统 QA PASS**。

## 2. 接入前基线

- 既有自动化测试：21 项通过。
- 原 5 个核心 API 与已有 7 个扩展 API：HTTP 200。
- 前端 8 个页面：正常；控制台无明显错误。
- 临时薄兼容图谱：72 个节点、74 条岗位—技能关系、3 个岗位、69 个技能。
- 动态演化：`not_connected`，页面显示“动态演化数据尚未接入”。
- 完整基线：`reports/formal_graph_evolution_baseline.json`。

## 3. 正式数据一致性

- `qa_report_v1.json`：`passed=true`。
- 正式图谱：490 个节点、2012 条关系、633 条岗位—技能关系，与组员 A QA 数量完全一致。
- `knowledge_graph_v1.json` 与 `graph_edges_v1.xlsx`：633 条岗位—技能关系逐条一致，JSON-only 0、Excel-only 0。
- 633 条岗位—技能关系全部包含至少一个真实 `evidence_jd_ids`，Evidence 未丢失。
- 三个重点岗位均存在，子图关系数分别为 37、21、17。
- 三个重点岗位的 JSON 演化记录均存在于 `job_skill_evolution_v1.xlsx`，缺失记录数均为 0。

## 4. 正式动态演化

- 时间范围：2026-04-14 至 2026-08-15。
- AI Agent开发工程师：12 条支持 JD；快速增长 0、新增 15、稳定 7、下降 14、样本不足 0。
- RAG引擎研发工程师：2 条支持 JD；正式状态汇总为样本不足 15，未输出其他趋势分类。
- AI安全技术工程师：9 条支持 JD；正式状态汇总为样本不足 15，未输出其他趋势分类。
- 页面按岗位独立展示，并保留科学性说明、样本量、时间范围和样本不足提示。
- 未重新计算或生成任何趋势。

## 5. API 回归

以下接口真实调用全部 HTTP 200：

- `POST /api/jd/parse`
- `POST /api/resume/parse`
- `POST /api/match`
- `GET /api/jobs`
- `GET /api/skills`
- `GET /api/system/overview`
- `GET /api/job-analysis/{job_title}`
- `GET /api/graph/job/{job_title}`
- `GET /api/graph/skill/{skill_id}`
- `GET /api/evolution/job/{job_title}`（三个重点岗位均验证）
- `GET /api/emerging-jobs`
- `GET /api/emerging-jobs/{candidate_id}`

OpenAPI 中原 5 个接口路径仍存在，JD、简历和匹配响应字段与接入前一致。

## 6. 前端回归

8 个页面全部正常：数据驾驶舱、岗位分析、岗位能力图谱、动态演化、新岗位发现、JD解析、简历分析、人岗匹配。图谱三个岗位均正常渲染 ECharts；动态演化不再显示“数据尚未接入”，三个岗位均能切换。中文正常、无横向溢出、浏览器控制台无警告或错误。

后端关闭场景已验证：图谱和演化页读取正式静态降级数据，分别显示“已读取程序导出的正式图谱兼容JSON”和“已读取静态正式演化结果”，数据与岗位切换仍正常。

## 7. 现场演示流程回归

- JD 文本 → JD Parser → 标准岗位、技能、Evidence：通过。
- 简历文本 → Resume Parser → 能力画像与技能 Evidence：通过。
- 简历 + 岗位 → Matching → 五维评分、技能差距、学习路径：通过。

新岗位结果仍为 11 个（高/中/弱：0/1/10），Evidence 校验通过，本阶段未重新运行或修改评分。

## 8. 冻结数据与正式产物哈希

四份冻结正式 Excel 接入前后 SHA-256 完全一致：

- `standardized_jd_dataset_v1.xlsx`：`B00A0220FD4B974D8B00BB57D6F0AF3BB40F1D92CC7DDD59FCB0DDDA9FC90EDE`
- `standard_job_title_mapping_v1.xlsx`：`293B34DBB8E4E6F5689CF58387A38601F30FEB759B46E7F34931BC1F6FF859B1`
- `standard_skill_dictionary_v1.xlsx`：`178C64E654D3534878489E88AAFC5A17B98FE361CB38DB370F9036D01E5C1055`
- `standardized_resume_testset_v1.xlsx`：`3B78EDFFD349055342818FFF6B803B92D5BD1C1BD1DE140E3764EF82C3CCD952`

组员 A 七个关键正式产物的接入后哈希与解包前 ZIP 条目哈希也完全一致。

## 9. QA 结果

- 自动化测试：22/22 通过。
- React + Vite 构建：通过。
- API：全部通过。
- 浏览器 8 页：全部通过。
- 正式结果一致性：通过。
- 冻结数据保护：通过。
- 三条现场流程：通过。

最终结论：**System V1 正式图谱/动态演化接入 QA PASS**。

## 10. 已知问题

- 有效发布时间覆盖率为 46.37%，正式数据不支持多年长期产业趋势解释。
- RAG 支持 JD 仅 2 条；RAG 与 AI 安全岗位的当前比较窗口均被正式结果标记为样本不足。
- 正式岗位—技能关系未提供 Evidence 原文，因此页面只显示真实 JD 编号。
- Vite 构建提示 ECharts 主包较大，但不影响本地比赛演示。
